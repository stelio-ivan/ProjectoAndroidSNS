<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dominando.android.mysnsserviodeurgncia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Mapa" />
        <activity android:name=".Historico_detalhe_activity" />
        <activity android:name=".HistoricoListActivity" />
        <activity android:name=".hospital_detalhe_activity" />
        <activity android:name=".HospitalListActivity" />
        <activity android:name=".cabecalho_navegacao" />
        <activity android:name=".sugestao_form" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>